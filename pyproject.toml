[project]
name = "adaspy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dspy-ai>=2.4.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
